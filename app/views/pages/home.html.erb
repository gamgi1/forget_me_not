
<div class="jumbotron">
  <h1>Hello, world!</h1>
  <h3>Welcome to Forget Me Not</h3>
  <p>A suitcase buddy, who never forgets a thing. Making both packing and life just a little easier.</p>
    <% if user_signed_in? %>
      <h1 class='page-header'>Welcome, <%= current_user.username.capitalize %></h1>
    <% else %>
      <p><a class="btn btn-primary btn-lg" href="<%= signup_path %>" role="button">SignUp!</a></p>
      <p><a class="btn btn-info btn-lg" href="<%= login_path %>">Login!</a></p>
    <% end %>
  </div>
